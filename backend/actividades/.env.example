# Copia este archivo a ".env" y ajusta los valores según tu entorno.

# Misma base de datos que "login" y "alumnos".
DATABASE_URL="postgresql://postgres:contrasena@localhost:5432/Bookie"

# Debe ser EXACTAMENTE el mismo valor que en el servicio "login".
JWT_SECRET="cambia-esto-por-un-secreto-largo-y-aleatorio"

# Puerto en el que corre este servicio.
PORT=3002
